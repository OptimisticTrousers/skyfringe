import express, { Router } from "express";
// import post_controller from "../controllers/postController";

const router: Router = Router();

// router.route("/", post_controller.post_list);

export default router;