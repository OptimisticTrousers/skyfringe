import { Request, Response, NextFunction } from "express";
import Comment from "../models/comment";

export const comment_list = (req: Request, res: Response, next: NextFunction) => {

}

export const comment_create = []

export const comment_update = []

export const comment_delete = (req: Request, res: Response, next: NextFunction) => {
  
}